using UnityEngine;

public class Test : MonoBehaviour, IPossessable
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
